import { FC } from 'react';

const Chat: FC = () => {
  return (
    <div>
      {' '}
      <h4 className="font-medium text-2xl md:text-[32px] text-text">Chat</h4>
    </div>
  );
};

export default Chat;
