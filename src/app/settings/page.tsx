import { FC } from 'react';

const Settings: FC = () => {
  return (
    <div>
      {' '}
      <h4 className="font-medium text-2xl md:text-[32px] text-text">
        Settings
      </h4>
    </div>
  );
};

export default Settings;
